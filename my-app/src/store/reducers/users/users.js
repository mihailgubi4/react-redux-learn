const initialState = [
        {id: 1, name: 'John'},
        {id: 2, name: 'Michael'},
        {id: 3, name: 'Anna'}
        ];

export function usersReducer(state = initialState) {
    return state;
}
